name: ci

# Continuous integration: build the self-contained `mdv` executable on every
# supported platform and run the test suite. Runs on pushes and pull requests so
# breakages are caught before a release tag is cut. No signing or publishing
# happens here — see release.yml for signed, notarized release artifacts.

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Quality gate. This job is the required status check for pull requests: it
  # runs `go vet` and the full unit + end-to-end test suite (with the race
  # detector) on the pure-Go packages, which need no native webview toolchain.
  # The platform build matrix below only runs once this gate is green.
  # ---------------------------------------------------------------------------
  test:
    name: test (quality gate)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: go.sum

      - name: Install Linux WebKitGTK (build-time only)
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libwebkitgtk-6.0-dev

      - name: go vet
        # Install the Linux webview toolchain here so the quality gate exercises
        # the same GUI packages that local builds and the platform matrix use.
        run: go vet ./...

      - name: Check gofmt
        run: |
          unformatted="$(gofmt -l $(git ls-files '*.go'))"
          if [ -n "$unformatted" ]; then
            echo "::error::These files are not gofmt-formatted:"
            echo "$unformatted"
            exit 1
          fi

      - name: Run unit + e2e tests (race + coverage)
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Coverage summary
        run: go tool cover -func=coverage.out | tail -1

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage.out
          retention-days: 7

  build:
    name: build
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: macos-darwin-universal
          - os: windows-latest
            target: windows-x64
            arch: amd64
          - os: windows-11-arm
            target: windows-arm64
            arch: arm64
          - os: ubuntu-latest
            target: linux-x64
          - os: ubuntu-24.04-arm
            target: linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache-dependency-path: go.sum

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: gui/frontend/package-lock.json

      - name: Install Linux WebKitGTK (build-time only)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libwebkitgtk-6.0-dev

      - name: Install Wails v3 CLI (Windows only)
        # scripts/build.ps1 calls wails3 directly; scripts/build.sh tolerates a
        # missing CLI (generation is best-effort). Limit installation to Windows
        # so Linux runners do not compile Wails CLI cgo dependencies.
        if: runner.os == 'Windows'
        run: go install github.com/wailsapp/wails/v3/cmd/wails3@v3.0.0-alpha2.103

      - name: Go test
        shell: bash
        run: go test ./...

      - name: Build (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: scripts/build.sh "${GITHUB_REF_NAME}"

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: pwsh scripts/build.ps1 -Version "$env:GITHUB_REF_NAME" -Arch "${{ matrix.arch }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: mdv-${{ matrix.target }}
          path: |
            build/mdv
            build/mdv.exe
          if-no-files-found: error
          retention-days: 7
